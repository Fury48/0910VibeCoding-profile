# DESIGN

## 이게 뭔가요

최동주의 개인 소개 페이지입니다. 이름, 소속, 소개 글, 관심사, 이전 프로젝트, 연락/SNS 링크를
한 화면에 보여주는 1페이지짜리 프로필 사이트입니다. 90년대 CRT 터미널을 흉내 낸 화면으로,
콘텐츠는 `cat profile.txt` 명령을 실행한 결과처럼 표시됩니다.

## 화면 구성

- **터미널 창**: 화면 전체가 하나의 터미널 창처럼 보이는 박스 안에 들어 있음
  - **타이틀 바**: 좌측에 빨강/노랑/초록 신호등 버튼 3개 (macOS 터미널 창 흉내), 우측엔 `guest@introserver:~` 표시
- **프롬프트 라인**: `guest@introserver:~$ cat profile.txt`
- **NAME / AFFIL**: 이름과 소속(학교·학부·학번)
- **INTRO**: 소개 문단
- **INTERESTS**: 관심사 목록 (`- 항목` 형태)
- **PROJECTS**: 이전 프로젝트 목록. 제목(링크가 있으면 클릭 가능)과 한 줄 설명(`└` 기호로 들여쓰기)
- **LINKS**: GitHub, Email 등 외부 링크 목록 (라벨: URL 형태)
- **커서**: 맨 아래 줄에 깜빡이는 커서(`█`)로 프롬프트가 다시 대기 중인 느낌을 줌

## 색과 글꼴

- **배경**: 거의 검정에 가까운 짙은 녹색조 검정 (`#0b0f0a`)
- **기본 글자색**: 형광 초록 (`#33ff66`), 은은한 발광 효과(text glow) 적용
- **보조/라벨 글자색**: 어두운 초록 (`#1f9c44`) — 섹션 제목, 프롬프트 기호, 설명 줄 등에 사용
- **신호등 버튼**: 빨강(`#ff5f56`), 노랑(`#ffbd2e`), 초록(`#27c93f`)
- **화면 효과**: 스캔라인(가로줄 반복 패턴)과 화면 가장자리를 어둡게 하는 비네트 오버레이로 옛날 브라운관 모니터 느낌 연출
- **글꼴**: 모노스페이스 — Geist Mono, 대체 글꼴로 Courier New / Courier
